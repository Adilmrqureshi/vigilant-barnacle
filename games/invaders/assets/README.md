# Invaders assets

Invaders renders everything with rectangles, so it ships no image or audio assets.
This directory exists because the deploy workflow copies `games/<name>/assets`
for every built game.
